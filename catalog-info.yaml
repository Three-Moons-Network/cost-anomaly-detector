apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: cost-anomaly-detector
  description: >
    AWS cost anomaly detection — daily Cost Explorer polling, 30-day rolling baseline, Claude-powered analysis and recommendations, SNS alerts.
  annotations:
    github.com/project-slug: Three-Moons-Network/cost-anomaly-detector
  tags:
    - ai
    - aws
    - cost-explorer
    - finops
    - lambda
    - python
    - terraform
spec:
  type: service
  lifecycle: experimental
  owner: user:linuxlsr
